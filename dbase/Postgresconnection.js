/*
const {pgdb_host, pgdb_name, pgdb_port, pgdb_username, pgdb_password} = require('../config');
const pgp = require('pg-promise')({
    capSQL: true // capitalize all generated SQL
});
const db = pgp(`postgres://${pgdb_username}:${pgdb_password}@${pgdb_host}:${pgdb_port}/${pgdb_name}`);

module.exports = db;
*/